[tool.poetry]
name = "idf"
version = "0.1.0"
description = "Sistema IDF"

[tool.poetry.dependencies]
python = "^3.10"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.packages]
include = [
    { path = "src", from = "." },
]
